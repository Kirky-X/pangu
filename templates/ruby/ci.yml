name: CI
on:
  push:
    branches: [main, master]
  pull_request:
permissions:
  contents: read
concurrency:
  group: ci-ruby-${{ github.ref }}
  cancel-in-progress: true
jobs:
  quality:
    name: Quality (RuboCop)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
      - run: bundle exec rubocop
  test:
    name: Test + Coverage (≥80%)
    runs-on: ubuntu-latest
    env:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      COVERAGE: "true"
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
      - run: bundle exec rspec
      - name: Enforce 80% line coverage
        run: |
          cov=$(ruby -rjson -e '
            f = "coverage/.last_run.json"
            if File.exist?(f)
              d = JSON.parse(File.read(f))
              pct = d.dig("result","covered_percent") || d.dig("result","line") || 0
              print pct.to_f
            else
              print 0
            end
          ' 2>/dev/null || echo 0)
          echo "Line coverage: ${cov}%"
          # 覆盖率门禁：<80% 即失败（不以 || true 中和）
          awk -v c="$cov" 'BEGIN{exit !(c+0 < 80)}' && \
            { echo "::error::Coverage ${cov}% < 80%"; exit 1; }
      # 原 curl|bash uploader 已退役（见 go/ci.yml 注释），改官方 action
      - name: Upload coverage
        if: ${{ env.CODECOV_TOKEN != '' }}
        uses: codecov/codecov-action@v4
        with:
          files: coverage/.resultset.json
  security:
    name: Security (Brakeman + bundle-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
      - name: Brakeman（Rails 应用，对应 Ruby→Brakeman）
        run: |
          bundle exec brakeman --no-exit-on-warn 2>/dev/null || \
            echo "::notice::Brakeman 仅对 Rails 应用生效，非 Rails 项目自动跳过"
      - name: bundle-audit（依赖 CVE）
        run: bundle exec bundle-audit check --update
        continue-on-error: true  # 显式 advisory：报告漏洞不阻断（不再用 || true 掩盖工具本身故障）
